version: "2"
sql:
  - engine: sqlite
    queries: db/queries
    schema: db/schema.sql
    gen:
      go:
        package: db
        out: internal/db
        overrides:
          - column: "invoices.next_action_on"
            nullable: true
          - column: "settings.sim_now"
            nullable: true
